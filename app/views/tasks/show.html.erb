<h2><%= @task.name %></h2>
<p><%=@task.done ? '☑' : '☐'%> Due to <%= @task.deadline %></p>
<%= link_to 'Edit', edit_task_path %>
<%= link_to 'Delete', task_path, method: :delete, data: { confirm: 'Are you sure ?'} %>
